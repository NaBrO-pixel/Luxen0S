---
# Remove the live session's account from the installed system. It must match
# the live USERNAME in auto/config (default: user). The person doing the
# installation creates their own account on the "users" page.
username: user
