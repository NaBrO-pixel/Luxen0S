# Minimal LuxenOS Plymouth script theme: branded background + a centered
# spinner, so boot still gives visual feedback without extra assets.

Window.SetBackgroundTopColor(0.114, 0.137, 0.188);    # #1d2330
Window.SetBackgroundBottomColor(0.180, 0.212, 0.314); # #2e3650

logo.image = Image("luxenos-background.png");
logo.sprite = Sprite(logo.image);
logo.sprite.SetX(Window.GetWidth()  / 2 - logo.image.GetWidth()  / 2);
logo.sprite.SetY(Window.GetHeight() / 2 - logo.image.GetHeight() / 2);

spinner.frame = 0;
fun refresh_callback() {
    spinner.frame++;
}
Plymouth.SetRefreshFunction(refresh_callback);
